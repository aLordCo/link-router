## What does this PR do?

A short description of the change and why it's needed.

## Type of change

- [ ] Bug fix (non-breaking)
- [ ] New feature (non-breaking)
- [ ] Breaking change (may break existing setups)
- [ ] Refactor / performance
- [ ] Docs
- [ ] CI / build / tooling
- [ ] Other

## How was it tested?

- [ ] `cargo check` and `cargo clippy --all-targets -- -D warnings` pass
- [ ] `cargo test` passes
- [ ] `npm run check` (svelte-check + tsc) passes
- [ ] Tested the bundled app at least on one platform
- [ ] CI passes on all platforms (Linux, macOS, Windows)

Platforms verified:

## Screenshots (if UI change)

Add before/after screenshots for UI changes.

## Related issues

Closes #…

## Checklist

- [ ] Code follows the existing style (no added comments, idiomatic Rust/Svelte)
- [ ] If user-facing, the UI text is in both `es` and `en` (see `src/lib/i18n`)
- [ ] CHANGELOG has an entry under a `## [Unreleased]` section
- [ ] No secrets or credentials introduced